---
title: インストール
description: システムへの Claudex のインストール方法
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

## インストール方法

<Tabs>
  <TabItem label="ワンライナー" icon="terminal">
    ```bash
    curl -fsSL https://raw.githubusercontent.com/StringKe/claudex/main/install.sh | bash
    ```
    インストールスクリプトは OS とアーキテクチャを自動検出し、GitHub から最新リリースをダウンロードして `~/.local/bin/claudex` にインストールします。

    インストールディレクトリを変更する場合:
    ```bash
    CLAUDEX_INSTALL_DIR=/usr/local/bin curl -fsSL https://raw.githubusercontent.com/StringKe/claudex/main/install.sh | bash
    ```
  </TabItem>
  <TabItem label="Cargo" icon="seti:rust">
    ```bash
    cargo install --git https://github.com/StringKe/claudex
    ```
    Rust ツールチェーンが必要です。バイナリは `~/.cargo/bin/claudex` にインストールされます。
  </TabItem>
  <TabItem label="GitHub Releases" icon="github">
    [GitHub Releases](https://github.com/StringKe/claudex/releases) からプラットフォーム向けのビルド済みバイナリをダウンロードしてください。

    利用可能なターゲット:
    - `x86_64-unknown-linux-gnu`
    - `x86_64-unknown-linux-musl`
    - `aarch64-unknown-linux-gnu`
    - `aarch64-unknown-linux-musl`
    - `x86_64-apple-darwin`
    - `aarch64-apple-darwin`
    - `x86_64-pc-windows-msvc`

    バイナリを展開して `PATH` に配置してください:
    ```bash
    tar xzf claudex-v*.tar.gz
    chmod +x claudex
    mv claudex ~/.local/bin/
    ```
  </TabItem>
</Tabs>

## システム要件

| 要件 | 詳細 |
|------|------|
| **OS** | macOS (Intel / Apple Silicon)、Linux (x86_64 / ARM64)、Windows (x86_64) |
| **Claude Code** | インストール済みで `claude` として PATH からアクセス可能であること |
| **任意** | スマートルーティングとコンテキストエンジン機能用に [Ollama](https://ollama.com) |

## インストールの確認

```bash
claudex --version
```

## 次のステップ

インストール後、[最初のプロバイダープロファイルを設定](/ja/configuration/)してください。
